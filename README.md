# Pi Workspace entry

Static HTTPS entry point for pi.lukefriedrichs.com. Redirects to the personal Pi workspace. This repository contains no application code, credentials, files, or private workspace content.

Domain and DNS are managed at Namecheap. GitHub Pages only serves this redirect. Authentication and all workspace services run on the Raspberry Pi.
